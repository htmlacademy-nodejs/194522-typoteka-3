mixin date(date)
  -
    const formatDate = (dateString) => {
      const date = new Date(dateString);
      return date.toLocaleDateString();
    }

  time(class=attributes.class dateTime=date) #{formatDate(date)}
